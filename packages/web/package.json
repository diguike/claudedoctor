{
  "name": "@claudedoctor/web",
  "version": "0.0.0",
  "private": true,
  "description": "Claude Doctor evidence-first browser check and open-source project site.",
  "type": "module",
  "scripts": {
    "dev": "pnpm run build && node serve.mjs",
    "preview": "node serve.mjs",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "@claudedoctor/core": "workspace:*"
  }
}
