{
  "name": "@claudedoctor/web",
  "version": "0.0.0",
  "private": true,
  "description": "Claude Doctor web — static preview now; Astro static site to follow (M3).",
  "type": "module",
  "scripts": {
    "dev": "node serve.mjs",
    "preview": "node serve.mjs",
    "build": "echo \"static page lives in public/; Astro build wiring is the next M3 step\""
  }
}
